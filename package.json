{
  "name": "forward",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "workspaces": [
    "front"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=front",
    "build": "npm run build --workspace=front",
    "preview": "npm run preview --workspace=front",
    "format": "npm run format --workspace=front"
  }
}

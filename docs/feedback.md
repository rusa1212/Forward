# R&D Monitor 회의 피드백 정리

> 회의에서 나온 피드백을 정리하고, 현재 프로젝트에서 수정/확인이 필요한 작업 목록으로 재구성한 문서.

## 1. 공고 수집 범위 확대

- 현재는 진행 중인 공고만 수집하고 있음. 마감된 공고도 일정 기간 함께 조회할 수 있도록 범위 확대 검토.
- 기준(안): 최근 3개월 이내 마감된 공고까지 함께 불러오는 방향.
- 마감 공고와 진행 중 공고는 상태값으로 명확히 구분되어야 함 (예: `진행중` / `마감` / `기한미정`).

**TODO**
- [x] 수집 대상 기간을 "진행 중 + 최근 3개월 이내 마감"으로 확대
  - `back/app/services/collector.py`에 `RECENT_CLOSED_DAYS = 90` 도입, K-Startup/나라장터 수집기가
    모집중 공고뿐 아니라 최근 90일 이내 마감된 공고까지 함께 담도록 수정 (`fetch_kstartup`,
    `fetch_bid_public_info`). 나라장터 조회 기간(`BID_LOOKBACK_DAYS`)도 90일로 맞춤.
- [x] 공고 상태값(진행중/마감/기한미정) 스키마 및 분류 로직 점검
  - `back/app/api/v1/announcements.py`의 `statusLabel` 계산 로직(접수중/접수예정/마감임박/마감/기한미정)이
    이미 접수시작일·종료일 기준으로 정확히 동작함을 확인 (수정 불필요).
- [x] 프론트에서 상태값별 필터/표시 UI 확인
  - `front/src/pages/SearchPage/SearchPage.tsx`의 상태 필터 기본값이 `전체`라 마감 공고가 늘어나도
    기존 화면이 깨지지 않고, `StatusBadge` 컴포넌트가 `statusLabel`을 그대로 배지로 표시함을 코드로 확인.

## 2. 나라장터 데이터 연동 여부 확인

- 나라장터 공고가 정상적으로 수집되지 않는 것으로 의심됨.
- 크롤링/API 연동 여부 및 수집 로직 확인 필요.
- 실제로 데이터 누락이 발생하는지 테스트 후 수정.

**TODO**
- [x] 나라장터 수집 로직(크롤링 or API) 현황 확인
  - 크롤링이 아니라 조달청 나라장터 입찰공고정보 API(`BidPublicInfoService`) 호출 방식
    (`back/app/services/collector.py`의 `fetch_bid_public_info`)임을 확인.
- [x] 실제 수집 결과와 나라장터 원본 데이터 비교 테스트
  - 로컬 `.env`의 실제 API 키로 라이브 호출해본 결과 **실제 버그 2건을 발견**:
    1. 이 API는 조회기간(inqryBgnDt~inqryEndDt)이 **31일을 넘으면 무조건 실패**
       (`resultCode: 07`, "입력범위값 초과 에러")하는데, 이 에러 응답이 `.get("response", {})`로
       조용히 빈 값 취급되고 있어서 큰 기간을 조회하면 0건으로 수집되고도 아무 로그도 안 남음.
    2. 나라장터 응답 페이로드가 다른 두 소스보다 훨씬 커서, 요청 하나가 기존 타임아웃(15초)을
       넘겨 `ReadTimeout`이 나는 경우를 실측으로 확인. 이때 `asyncio.gather`가 예외를 그대로
       전파해 나라장터뿐 아니라 K-Startup/과기정통부까지 이번 수집 사이클 전체가 실패했음
       — "나라장터가 수집 안 된다"는 회의 피드백의 실제 원인으로 보임.
- [x] 누락 확인 시 원인 파악 및 수정
  - (1) 조회기간을 API 상한(31일) 이하 구간으로 쪼개 병렬 호출 후 병합하도록 수정
    (`_split_bid_windows`), 상한 초과 에러는 `BidApiError`로 명확히 드러나게 함(`_bid_response_body`).
  - (2) 나라장터 전용 타임아웃을 25초로 늘리고(`BID_TIMEOUT`), 구간 하나·소스 하나가
    실패해도 나머지는 정상 저장되도록 `fetch_bid_public_info`/`collect_all`을
    `asyncio.gather(..., return_exceptions=True)` + 로깅으로 격리.
  - 로컬 DB로 백엔드 테스트 전체(98건) 통과 확인. 실제 API로 90일치 재수집도 검증 —
    25,086건 정상 수집. 검증 도중 구간 하나가 API 호출 한도(429 Too Many Requests)에
    걸렸는데, 그 구간만 로그를 남기고 스킵되고 나머지 구간은 정상 반환되는 것까지
    실제로 확인해 방어 로직이 의도대로 동작함을 검증함.

## 3. 저장한 공고 접근성 개선

- 사용자가 저장한 공고를 따로 확인하기 어려운 상태.
- 대시보드의 "저장한 공고" 영역 클릭 시 저장 목록 화면으로 이동하도록 개선.
- 마이페이지 또는 홈 화면에서도 저장 공고를 쉽게 확인할 수 있도록 구성.
- 핵심은 저장 기능 자체보다 저장 후 "다시 찾아가는" UX를 명확히 하는 것.

**TODO**
- [x] 대시보드 "저장한 공고" 카드/영역 클릭 시 저장 목록으로 이동하는 링크 연결
  - `front/src/pages/DashboardPage/StatsGrid.tsx`의 "저장한 공고" KPI 카드를 버튼으로 바꿔
    클릭 시 `/mypage/saved`로 이동하도록 수정.
- [x] 마이페이지 또는 홈 화면에 저장 공고 접근 경로 추가
  - 마이페이지에 "저장한 공고" 탭 신설(`front/src/pages/MyPage/SavedTab.tsx`, 라우트
    `/mypage/saved`). 검색 결과와 동일한 `ResultsTable`을 재사용해 상세보기/저장해제가
    바로 되게 함.
  - 백엔드 `GET /saved-announcements`가 `announcements.py`의 `_serialize()`를 그대로
    재사용하도록 정리해(`back/app/api/v1/saved_announcements.py`) dday/상태 등 상세 화면에
    필요한 필드를 누락 없이 내려주게 함.
- [x] 저장 → 재방문 흐름 전체 점검
  - 검색 결과에서 별 아이콘으로 저장 → 저장한 공고 탭에서 재확인 → 저장 해제 시 목록에서
    즉시 제거되는 흐름을 코드 레벨로 확인. (로컬에 DB가 없어 브라우저 실동작 확인은
    아직 못함 — 다음 로컬 실행 시 확인 필요)

## 4. 알림 중복 발송 여부 확인

- 동일 공고에 대한 알림이 중복으로 재발송되는지 확인 필요.
- 공고 ID, 키워드, 알림 유형 기준으로 중복 발송 방지 로직 점검.
- 아래 알림 유형별로 각각 확인:
  - 신규 공고 알림
  - 키워드 매칭 알림
  - 마감 임박 알림

**TODO**
- [ ] 알림 발송 이력(공고 ID + 키워드 + 알림 유형) 기준 중복 체크 로직 확인
- [ ] 신규 공고 / 키워드 매칭 / 마감 임박 알림 각각 중복 발송 테스트
- [ ] 중복 발송 확인 시 방지 로직 추가/수정

## 5. 전체 UX 플로우 점검

- 모든 버튼이 정상적으로 작동하는지 확인.
- 단순히 링크 연결 여부뿐 아니라, "이 버튼을 눌렀을 때 이동하는 화면이 사용자의 기대와 맞는가?"까지 검토.
- 불필요한 이동 단계나 헷갈리는 버튼 명칭도 함께 수정.

**주요 사용자 흐름**
- [ ] 공고 탐색 → 상세 공고
- [ ] 공고 저장 → 저장 공고 확인
- [ ] 키워드 등록 → 매칭 공고 확인
- [ ] 알림 클릭 → 해당 공고 상세
- [ ] 대시보드 KPI / Quick Action → 적절한 목록 화면 이동

**TODO**
- [ ] 위 5개 핵심 플로우 각각 실제 동작 및 기대 결과 일치 여부 테스트
- [ ] 불필요한 이동 단계 제거
- [ ] 헷갈리는 버튼 명칭 정리
